<!DOCTYPE html>
<html lang="en">
    <head>
      <%- include('./partials/head') %>
    <link rel="stylesheet" href="/css/index.css">
    </head>
<body>

  <%- include('./partials/header') %>
  <main>
    <section>
      <% users.forEach(function(user) { %>
        <article class="usuario">
          <a href="/products/detail/<%= user.id %>"> <div class="contenedor-foto-producto">
            
        </div>
        <div class="descripcion">
            <p class="nombre"> nombre: <%= user.firstName %></p>
            <p class="nombre"> apellido: <%= user.lastName %></p>
            <p class="nombre"> email: <%= user.email %> </p>
            <p class="nombre"> contraseña: <%= user.password %> </p>
        </div>
        </a>
        </article> 
        <% }) %>
    </section>
    
  </main>

  <%- include('./partials/footer') %>